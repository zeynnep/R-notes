# "ls()"  bu komut daha öncesinde atadığınız değerleri size listeler

# "rm()" silmek istediğiniz değeri rm nin içiine yazdığınızda listeden çıkartabilirsiniz.
 
 # mode() bu komut içine yazdığımız değerin hangi veri türünde olduğunu söyyler.
 